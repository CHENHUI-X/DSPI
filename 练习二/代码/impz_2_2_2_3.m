% y[n]=0.25（x[n-1]+x[n-2]+x[n-3]+x[n-4]）
%用impz()函数求解系统单位阶跃响应
%先将x[n]=单位阶跃序列带入
%得到S[n]=y[n] = 0.25*delta[n-1]+0.5*delta[n-2]+0.75*delta[n-3] + u[n-4]
%得到的序列就是单位脉冲序列表示的结果,而impz函数求得就是单位脉冲响应
%所以此时系统的单位脉冲响应结果就是
b=[0,0.25,0.5,0.75,ones(1,17)];
a=1;
impz(b,a,21)
set(gca,'YLim',[0 1.5]);%Y轴的数据显示范围